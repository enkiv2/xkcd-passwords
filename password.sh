#!/usr/bin/env zsh
echo  $(cat /usr/share/dict/words | sort -R | head -n 1)$(cat /usr/share/dict/words | sort -R | head -n 1)$(cat /usr/share/dict/words | sort -R | head -n 1)$(cat /usr/share/dict/words | sort -R | head -n 1)
